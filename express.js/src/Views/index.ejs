<%- include('partials/header'); %>

    <h1>
        <%= title %>
    </h1>

    <% if (isActive) { %>
        <p>El servidor esta activo</p>
    <% }else{ %>
        <p>El servidor esta inactivo</p>
    <% } %>
    <ul>
        <% for(let i = 0; i < users.length; i++) { %>
        
            <li><%= users[i].name %> <%= users[i].alias %> <%= users[i].id %></li>
        
      <% } %>
    </ul>

<%- include('partials/footer'); %>