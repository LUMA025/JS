function add(a, b) {
    return a + b
}

function sus(a, b) {
    return a - b
}

function div(a, b) {
    return a / b
}

function mul(a, b) {
    return a * b
}

module.exports = {
    add,
    sus,
    div,
    mul
}

// Aca simplemente exportamos funciones que utilizaremos en main para realizar operaciones matematicas.